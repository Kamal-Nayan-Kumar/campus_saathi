# Web Server
fastapi
uvicorn
python-multipart

# Core Framework
python-dotenv

# Telegram
python-telegram-bot

# RAG stack (ADR-0001): LangChain owns the chain + splitting,
# Groq hosts openai/gpt-oss-120b via its OpenAI-compatible API,
# Qdrant Cloud stores vectors with server-side embeddings.
langchain
langchain-openai
langchain-qdrant
langchain-text-splitters
qdrant-client

# Document parsing — dual: Firecrawl (cloud) + local fallbacks
firecrawl
pypdf
openpyxl
python-docx

# Dev / test
pytest
httpx
