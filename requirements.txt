# Web Server
fastapi
uvicorn
python-multipart

# Core Framework
python-dotenv

# Telegram
python-telegram-bot

# RAG stack (ADR-0001): LangChain owns the chain + splitting,
# Groq hosts openai/gpt-oss-120b via its OpenAI-compatible API,
# Qdrant Cloud stores vectors with server-side embeddings.
langchain
langchain-openai
langchain-qdrant
langchain-text-splitters
qdrant-client

# PDF parsing
firecrawl

# Dev / test
pytest
httpx
